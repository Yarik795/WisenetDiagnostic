# Референс: HRX-1634 @ 10.89.215.130 (нет HDD)

Санитизированный дамп SUNAPI (2026-05-27). Кейс: **отсутствует накопитель**, запись невозможна; до доработки классификаторов UI показывал «Исправно» и не включал NVR в отчёт об ошибках.

| Параметр | Значение |
|----------|----------|
| Модель | HRX-1634 |
| Имя устройства | VAV23-2post |
| Прошивка | 5.31.42_231023102331 |
| CGI | 2.6.0 |
| IP | 10.89.215.130 |
| Каналов | 18 (13 активных, 5 Deactive/Disconnected) |

## Сводка для мониторинга

| Категория | Факт с устройства | Ожидаемый статус после доработки |
|-----------|-------------------|----------------------------------|
| Накопители | `HDDNone=True`, пустой `storageinfo` | `error` |
| Архив | `searchrecordingperiod` → NG 604 | `error` |
| Запись | `recording-storage` → `Enable=False` | учитывается в «Накопители» |

## storageinfo

```
GET http://10.89.215.130/stw-cgi/system.cgi?msubmenu=storageinfo&action=view
HTTP 200

Status=Normal
```

Слотов `Storage.N.*` нет. `worst_status` в парсере не выставляется из корневого `Status`.

## diskutility

```
(empty body)
```

## recording-storage

```
Enable=False
OverWrite=False
AutoDeleteDays=400
```

## searchrecordingperiod

```
NG
Error Code: 604
Error Details:
Invalid Input Value(s)
```

## eventstatus (SystemEvent)

```
SystemEvent.HDDFail=False
SystemEvent.HDDError=False
SystemEvent.HDDNone=True
SystemEvent.HDDFull=False
SystemEvent.Recording=False
SystemEvent.InternalHDDConnect.1=False
SystemEvent.InternalHDDConnect.2=False
```

## Сравнение с HRX-1634 с HDD

См. `docs/nvr-api-check.txt` (10.89.210.104): `Storage.1.Model`, `HDDNone=False`, рабочий `searchrecordingperiod`.

## Вывод для разработки

- Ключевой сигнал отсутствия диска: **`SystemEvent.HDDNone=True`**, не `HDDFail`/`HDDError`.
- Пустой `storageinfo` при `storageinfo_ok` → «Накопители не обнаружены».
- Код **604** на архиве при отсутствии HDD — отдельный `archive_poll_error`.
